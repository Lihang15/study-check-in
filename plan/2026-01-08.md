# 任务安排 - 交作业时间 不能晚于1月11日晚上10点前，

## 需求1： 实现一个post请求，创建一条task数据，把用户数据通过这个方法插入到数据库

路由为/createTasks，，需要接收用户数据    id, task_name，task_content，status
成功创建，返回给前端 {status: true}，失败返回{status: false}
 
这是service/task.py  数据处理层已经实现
 def create_task(self, task_data: Dict) -> TaskModel:

## 需求2，把整个项目跑起来 在本地看效果，，跑项目的命令都在README.md里，，
 把前端和后端跑起来的截图，放在这里


